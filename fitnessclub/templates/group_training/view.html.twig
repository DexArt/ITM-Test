{% extends 'base.html.twig' %}

{% block title %}GroupTraining{% endblock %}

{% block body %}
    <div class="site-wrap">

        <div class="site-section">
            <div class="container text-center" style="width: 100%; height: 300px">
                <div class="col-md-4 text-center mb-4" style="width: 100%; height: 300px !important; max-width: 100% !important;">
                    <div class="border p-4 text-with-icon" style="height: 300px">
                        <h2 class="h5" style="text-transform: uppercase"><strong>{{ training.name }}</strong></h2>
                        <p>Description</p>
                        <p>{{ training.description }}</p>
                        <p><a href="{{ path('subscribeGroupTraining',{'id':training.id}) }}">Subscribe</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
